name: Fedora
description: Fedora Linux platform terms — D-Bus interface model, GNOME Shell, and systemd concepts used in Borshevik.
extends: @DuckspecProject
uses:
  - ../duckspec/Duckspec.yaml
settings:
  terms_folder: Fedora/
  src: ..
