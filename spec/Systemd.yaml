name: Systemd
description: The systemd service manager as it is used in Borshevik — the @Systemd application and all @SystemdUnit extensions it hosts.
extends: @DuckspecProject
uses:
  - ../duckspec/Duckspec.yaml
settings:
  terms_folder: Systemd/
software:
  - extends: @SystemdManager
  - extends: @SystemdUnit
