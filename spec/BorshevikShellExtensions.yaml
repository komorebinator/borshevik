name: BorshevikShellExtensions
description: GNOME Shell extensions installed in Borshevik at image build time — the installer script, extension manifest, patches, and custom build scripts.
extends: @DuckspecProject
uses:
  - ../duckspec/Duckspec.yaml
  - Fedora.yaml
  - https://github.com/komorebinator/borshevik-workspace-manager
  - https://github.com/komorebinator/borshevik-app-search
settings:
  terms_folder: BorshevikShellExtensions/
  src: ..
software:
  - @GnomeExtensions
