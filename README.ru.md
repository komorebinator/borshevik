# <img src="assets/borshevik_logo.svg" alt="Logo" width="46"> Borshevik
[🇬🇧 English](README.md) | [🇷🇺 Русский](README.ru.md)


Borshevik — это неизменяемый (immutable) десктопный образ, ориентированный в первую очередь на ноутбуки. Он построен на Fedora Atomic и экосистеме uBlue и задуман как более функциональная альтернатива ChromeOS. Поставляется в виде единого образа системы “только для чтения” с атомарными обновлениями и простыми откатами, со стандартным GNOME из Fedora, настроенным небольшим набором предустановленных расширений GNOME Shell, поддержкой мультимедиа, Chrome и подобранным набором Flatpak-приложений — чтобы вы могли просто установить систему и сразу начать ей пользоваться, даже если вы новичок в Linux.

## 🎯 Для кого этот образ?

Borshevik — практичный daily driver с понятной целевой аудиторией:

- **В первую очередь для ноутбуков (но десктопы тоже поддерживаются):** настройки и UX заточены под современные ноутбуки, при этом на настольных ПК всё тоже работает отлично.
- **В первую очередь для работы (игры — опционально):** базовый образ ориентирован на повседневную продуктивность; игры поддерживаются, но не “вшиваются” в систему (например, Steam — через Flatpak).
- **«ChromeOS, но мощнее»:** тот же принцип «включил и работаешь», но с приложениями

## 🌸 Базовый образ

Borshevik собирается как единый read-only образ системы на базе Fedora Atomic при помощи экосистемы uBlue. Обновления приходят целиком: вы устанавливаете новый образ, а если что-то сломалось — можно просто загрузиться в предыдущую версию. Включены популярные мультимедийные кодеки, поэтому видео и аудио работают сразу после установки. Также есть отдельная сборка для NVIDIA с предустановленным проприетарным драйвером.

## 🌐 Chrome

Chrome предустановлен и ставится из официального RPM от Google. Он нативно работает на Wayland и поддерживает плавные жесты тачпада из коробки — без дополнительной настройки.

## 📦 Набор приложений

Небольшой фоновый сервис автоматически устанавливает подобранный набор приложений из Flathub после первого запуска. Полный список находится в [flatpaks.txt](build_files/root/usr/share/app-choice-subscription/flatpaks.txt). Удалённые вами приложения остаются удалёнными, а новые добавления из этого списка подтягиваются автоматически.

Набор покрывает повседневную работу, медиа, игры и коммуникации, поэтому большинству пользователей достаточно просто войти в систему и начать ей пользоваться сразу.

## 🧩 Расширения GNOME

Borshevik поставляется с набором расширений GNOME Shell, предустановленных на этапе сборки образа. Полный список и все патчи документированы в [list.json](build_files/scripts/gs-extensions/list.json), так что вы всегда можете увидеть, что именно включено.

Эти расширения добавляют небольшие улучшения качества жизни — историю буфера обмена, пипетку, картинку-в-картинке, твики рабочих столов и немного визуальных улучшений.

## 👷 Переезд (rebase) с другого uBlue

Если вы уже используете Fedora Atomic или другой uBlue-образ, переключиться на Borshevik можно одной командой rebase:

```bash
sudo rpm-ostree rebase ostree-image-signed:docker://ghcr.io/komorebinator/borshevik:stable
```

или для NVIDIA GPU:

```bash
sudo rpm-ostree rebase ostree-image-signed:docker://ghcr.io/komorebinator/borshevik-nvidia:stable
```

После rebase перезагрузитесь — и всё готово.

## 🛡️ Приватность и телеметрия

Borshevik не добавляет дополнительную телеметрию или трекинг поверх того, что уже есть в Fedora. Нет обязательных онлайн-аккаунтов, а системный образ полностью документирован и воспроизводим благодаря публичным файлам сборки в этом репозитории.

[![Download](assets/download-ru.svg)](https://borshevik.org/iso/borshevik-stable.iso)  
[![Download](assets/download-nvidia-ru.svg)](https://borshevik.org/iso/borshevik-nvidia-stable.iso)